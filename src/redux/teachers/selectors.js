export const selectTeachers = state => state.teachers.items;
export const selectIsLoading = state => state.teachers.isLoading;
export const selectTotalPages = state => state.teachers.pagination.totalPages;
